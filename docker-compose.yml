version: '3.3'

services:

    nginx:
        build: docker/nginx
        ports:
            - ${WEB_SERVER_PORT}:80
        volumes:
            - .:/var/www
        working_dir: /var/www
        networks:
            - local

    php:
        build: docker/php
        expose:
            - 9000
        volumes:
            - .:/var/www
        working_dir: /var/www
        networks:
            - local
        environment:
            XDEBUG_CONFIG: remote_host=${XDEBUG_REMOTE_HOST}
            PHP_IDE_CONFIG: serverName=${XDEBUG_SERVER_NAME}

    mysql:
        build: docker/mysql
        environment:
            - MYSQL_DATABASE=mmpi
            - MYSQL_USER=mmpi
            - MYSQL_PASSWORD=mmpi
            - MYSQL_ALLOW_EMPTY_PASSWORD=1
            - MYSQL_ROOT_HOST=%
        volumes:
            - ./storage/db/mysql:/var/lib/mysql
        networks:
            - local

    elastic:
        build: docker/elastic
        ports:
            - ${ELASTIC_SERVER_PORT}:9200
        volumes:
            - ./storage/db/elasticsearch/data:/usr/share/elasticsearch/data
        networks:
            - local

networks:
    local:
        driver: bridge
